
import 'package:flutter/material.dart';

class CurrencyIcon extends StatelessWidget {

  @override
  build(context) => Text('TZS',
    style: TextStyle(
      fontSize: 13,
      fontWeight: FontWeight.bold,
      color: Colors.grey.shade600
    )
  );
}